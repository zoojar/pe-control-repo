---
version: 5

defaults:
  datadir: "data"

hierarchy:
  - name: "Yaml backend"
    data_hash: yaml_data
    paths:
      - "nodes/%{trusted.certname}.yaml"
      - "app_tier/%{facts.app_tier}.yaml"
      - "os/%{facts.os.name}.yaml"
      - "common.yaml"
